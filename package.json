{
  "name": "rnl-dj-bot",
  "private": true,
  "version": "1.0.0",
  "description": "Discord DJ bot with a multi-operator web control surface",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w web && npm run build -w server",
    "typecheck": "node scripts/check-protocol.mjs && npm run typecheck -w server && npm run typecheck -w web",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
